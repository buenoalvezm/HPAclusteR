# Visualize cluster comparison results with a network graph

Visualize cluster comparison results with a network graph

## Usage

``` r
visualize_comparison_net(matched_clusters)
```

## Arguments

- matched_clusters:

  A tibble containing matched clusters between two datasets

## Value

A network graph plot
