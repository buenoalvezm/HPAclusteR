% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hc_consensus_cluster.R
\name{hc_consensus_cluster}
\alias{hc_consensus_cluster}
\title{Consensus clustering of genes}
\usage{
hc_consensus_cluster(
  AnnDatR,
  resolution,
  method = "louvain",
  n_seeds = 100,
  seed = 42,
  verbose = TRUE
)
}
\arguments{
\item{AnnDatR}{An AnnDatR object containing the data with SNN graph.}

\item{resolution}{A numeric vector of resolution parameters for clustering.}

\item{method}{Clustering method to use: "louvain" (default) or "leiden".}

\item{n_seeds}{Number of different random seeds to use for clustering (default is 100).}

\item{seed}{Random seed for reproducibility (default is 42).}

\item{verbose}{Logical indicating whether to print progress messages (default is TRUE).}
}
\value{
Consensus clustering results stored within the AnnDatR object.
}
\description{
Clusters genes using multiple runs of a clustering algorithm with different seeds and resolutions, then derives a consensus clustering.
}
\examples{
#' # Perform consensus clustering on an AnnDatR object after SNN graph computation
adata_res <- hc_pca(example_adata, components = 40)
adata_res <- hc_distance(adata_res, components = 20)
adata_res <- hc_snn(adata_res, neighbors = 15)
adata_res <- hc_consensus_cluster(adata_res, resolution = 6.3)
head(adata_res$uns$consensus_clustering)
head(adata_res$obs)
}
