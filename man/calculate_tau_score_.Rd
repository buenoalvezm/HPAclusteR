% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calculate_tau_score.R
\name{calculate_tau_score_}
\alias{calculate_tau_score_}
\title{Calculate Tau Score}
\usage{
calculate_tau_score_(df, gene_col)
}
\arguments{
\item{df}{A tibble containing gene expression values (genes as rows, samples (e.g. tissues, single cell types, etc.) as columns).}

\item{gene_col}{Name of the column in \code{df} that contains gene identifiers.}
}
\value{
A tibble with two columns:
\describe{
\item{gene}{Gene identifier.}
\item{tau_score}{Tau score for each gene.}
}
}
\description{
Computes the tissue specificity measure Tau (Yanai et al., 2005) for each gene
based on expression values across samples (e.g. tissues, single cell types, etc.). Tau ranges from 0 (ubiquitous expression)
to 1 (tissue-specific expression).
}
\examples{
library(dplyr)
library(tidyr)
example_data <- tibble::tibble(
  gene_id = c("gene1", "gene2"),
  tissueA = c(10, 2),
  tissueB = c(8, 0),
  tissueC = c(7, 0)
)
calculate_tau_score(example_data, gene_col = gene_id)
}
